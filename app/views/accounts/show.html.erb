<div class="">
<%= @message %>
<%= flash_message %>
  <h2 class=""><%= @account.iban%> </h2>
  <br>

  <section class="row">
    <div class="">
      <div class="col-md-6">
        <ul>
          <li> <strong>IBAN:</strong> <span class="" >
            <%= "#{@account.iban}" %></span>
          </li>
          <li> <strong>Amount:</strong> <span class="" >
            <%= "#{@account.amount}" %> </span>
          </li>
          <li> <strong>Holder:</strong>
            <%= "#{@account.holder}" %>
          </li>
          <br>
        </ul>
      </div>
    </div>
  </section>


  <br>
  <a href="<%= edit_bank_account_path(params[:bank_id],@account.id)%>" class="btn" >Edit</a>
  <br>
  <br>
  <a href="<%= bank_path(params[:bank_id]) %>">Back to Bank</a>
  <br>
  <br>
  <a href="<%= root_path %>">Back to Home</a>

</div>
